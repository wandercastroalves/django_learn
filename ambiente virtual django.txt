Ambiente virtual 
 pasta onde instala todas as dependencia de um projeto 

venv - gerenciador de ambiente virtual do python (padrão)

Criando ambiente virtual e instalar o django
   abriando a pasta no vscode
     code .caminho da pasta
	no vscod dentro da pasta do ambiente virtual crtl+shift+p - select interpreter - 
	
Criar chave ssh github
	criar repositorio 
	abrir terminal 
	git config --global user.name "wandercastroalves"
	git config --global user.email "wander@micronetbrasil.com"
	git config --global init.defaultBranch main
	git init

	ssh-keygen
	
